{
  "name": "ssb-keys",
  "version": "7.2.0-7",
  "description": "Rust-in-Node.js variant of ssb-keys",
  "main": "lib/index.js",
  "author": "Andre Staltz <andre@staltz.com>",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/staltz/ssb-neon-keys.git"
  },
  "dependencies": {
    "mkdirp": "^1.0.4",
    "neon-cli": "~0.4.1",
    "neon-load-or-build": "staltz/neon-load-or-build#thisone",
    "node-abi": "^2.19.1"
  },
  "devDependencies": {
    "electron-build-env": "^0.2.0",
    "ssb-keys": "7.2.0",
    "tape": "^3.0.3"
  },
  "scripts": {
    "install": "neon-load-or-build",
    "build-debug": "neon build",
    "check": "node check.js",
    "build-release": "neon build --release",
    "build-release-electron": "electron-build-env neon build --release",
    "tag-prebuild": "node tag-as-prebuild.js",
    "tag-prebuild-electron": "electron-build-env node tag-as-prebuild.js",
    "setup-compat-tests": "cp -RT ./node_modules/ssb-keys/test ./test-compat",
    "pretest": "npm run build-debug",
    "test-compat": "npm run setup-compat-tests && tape test-compat/*.js",
    "test-extra": "tape test-extra/*.js",
    "test": "npm run test-compat && npm run test-extra"
  },
  "binary": {
    "module_name": "index",
    "host": "https://github.com/staltz/ssb-neon-keys/releases/download/",
    "remote_path": "{version}",
    "package_name": "{node_abi}-{platform}-{arch}.tar.gz",
    "module_path": "./native",
    "pkg_path": "."
  }
}
